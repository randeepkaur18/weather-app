import React from "react";

import './Header.css';

const header = (props) => {
    return (
        <header className="header">
            WEATHER CHANNEL
        </header>
    );
}

export default header;